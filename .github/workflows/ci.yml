name: CI

on:
  push:
  pull_request:

jobs:
  lint-test-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build shared (pre-lint)
        run: pnpm --filter @bonusbridge/shared build

      - name: Generate Prisma Client (pre-lint)
        run: pnpm --filter api prisma:generate

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test

      - name: Build api
        run: pnpm --filter api build

      - name: Build web
        run: pnpm --filter web build

      - name: Build admin
        run: pnpm --filter admin build
